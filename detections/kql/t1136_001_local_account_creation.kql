DeviceProcessEvents
| where (FileName in~ ("net.exe", "net1.exe") and ProcessCommandLine has_any ("user /add", "user add"))
    or (FileName in~ ("powershell.exe", "pwsh.exe") and ProcessCommandLine contains "New-LocalUser")
| project Timestamp, DeviceName, AccountName, FileName, ProcessCommandLine
